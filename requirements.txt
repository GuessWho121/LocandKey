# Python 3.11: PyTorch training, preprocessing, evaluation and tests.
# Install the appropriate CUDA/CPU torch wheel first (see README.md).
# Transitive dependencies such as protobuf are resolved by the installer.
torch==2.12.0
tensorboard==2.21.0
numpy==1.26.4
matplotlib==3.10.8
tqdm==4.67.3
